# v0.0.4 PRD 验收条款 ↔ 实现矩阵（#13 全量回归底稿）

> QA 子 agent 编制（任务 #29）。依据：`docs/v0.0.4/需求.md` §19（验收标准，共 81 条，19.1–19.12）+ v0.0.3..HEAD 42 个 commit + 代码现状只读核查。
> 判定标准：**已实现-待回归** = 有实现+测试证据，待真机/回归验证；**部分实现** = 主干在但缺口明确（标注任务板编号）；**未实现**；**存疑** = 证据不足或口径未核对。
> apps/api 有 agent 在途（#26 等），本文档只做只读盘点，不改动任何代码。

## 统计汇总（共 81 条）

| 状态 | 计数 | 条款 |
|---|---|---|
| 已实现-待回归 | 74 | 1–36、38–45、47、49–57、59、60、62–79 |
| 部分实现 | 4 | 37、46、80、81 |
| 存疑 | 3 | 48、58、61 |
| 未实现 | 0 | — |

---

## 19.1 基础（1–5）

| # | 验收原文摘要 | 状态 | 证据 | 关联任务 |
|---|---|---|---|---|
| 1 | 双击桌面应用启动，托盘图标出现 | 已实现-待回归 | apps/desktop Tauri sidecar 打包链（`apps/desktop/src-tauri/resources/sidecar/`）；托盘/真机项待补验 | #25（真机项） |
| 2 | 无需登录直达主界面 | 已实现-待回归 | 63dae46（api 移除账号体系）、82b45a7（web 移除登录/账号/市场） | — |
| 3 | 浏览器可开 `http://127.0.0.1:7788/` | 已实现-待回归 | `apps/api/src/common/paths.ts:6`（DEFAULT_PORT=7788）、`apps/web/src/api/env.ts:15`、`paths.test.ts` | — |
| 4 | 关闭浏览器后服务继续运行 | 已实现-待回归 | sidecar 常驻形态（§1.2）；属真机验证项 | #25（真机项） |
| 5 | Agent 未接入时可正常创建/拖拽/配置任务 | 已实现-待回归 | 存量看板链路 + `apps/api/src/__tests__/state-machine.test.ts`、`acceptance-gaps.test.ts` | — |

## 19.2 分组（6–9）

| # | 验收原文摘要 | 状态 | 证据 | 关联任务 |
|---|---|---|---|---|
| 6 | 可创建/编辑/删除分组 | 已实现-待回归 | 7ed1f19（Project→Group + 0008 迁移 + 上限 50）、`groups/__tests__/groups.test.ts` | — |
| 7 | 默认分组不可删除 | 已实现-待回归 | 22835a0（预置默认分组与删除保护）、`groups.service.ts:144`（预置标记只认迁移植入） | — |
| 8 | 删除分组时任务迁移到默认分组 | 已实现-待回归 | `groups.service.ts:252-259`（migrate 策略，§5.5 默认分组为删除对话框默认项） | — |
| 9 | 分组管理在设置页 | 已实现-待回归 | 7ed1f19 + `apps/web/src/features/settings/tabs/`、`apps/web/src/features/groups/` | — |

## 19.3 任务管理（10–17，存量回归为主，见 §21.3）

| # | 验收原文摘要 | 状态 | 证据 | 关联任务 |
|---|---|---|---|---|
| 10 | 可创建/拖拽/配置任务 | 已实现-待回归 | 存量看板 + `apps/web/src/features/board/mutations.ts` | — |
| 11 | Agent 通过 MCP 领取任务 | 已实现-待回归 | `agent/__tests__/claim.test.ts`、`mcp-http-flow.test.ts`（存量 9 工具保留，§21.1） | — |
| 12 | 领取后自动进入执行中，看板实时更新 | 已实现-待回归 | `claim-lease-flow.test.ts` + WS 通道（`apps/web/src/ws/invalidate.ts`） | — |
| 13 | 完成后进入待审核 | 已实现-待回归 | `agent/__tests__/writeback.test.ts` | — |
| 14 | 用户审核必填建议/原因/详情 | 已实现-待回归 | 存量审核四字段（`schema.prisma:195 Review`）；§21.3 标「必填口径核对」为部分项，**回归时重点核对** | — |
| 15 | Agent 下次领取能读到审核意见 | 已实现-待回归 | `writeback.test.ts` + claim 读侧 | — |
| 16 | 两个 Agent 同时领取只有一个成功 | 已实现-待回归 | 原子认领（09421f5 提及双谓词模式沿用存量）+ `claim.test.ts` | — |
| 17 | 租约超时回到异常/失败 | 已实现-待回归 | `agent/__tests__/lease-sweeper.test.ts` | — |

## 19.4 依赖（18–21，存量回归）

| # | 验收原文摘要 | 状态 | 证据 | 关联任务 |
|---|---|---|---|---|
| 18 | 依赖未满足领不到 A | 已实现-待回归 | `jobs/dependency-unlock.service.ts` + `state-machine.test.ts` | — |
| 19 | B 完成后 A 可领 | 已实现-待回归 | 同上（依赖解锁 job） | — |
| 20 | 循环依赖被拒绝 | 已实现-待回归 | §21.3 存量「循环依赖检测」+ `contract/__tests__/transitions.test.ts` | — |
| 21 | 置顶任务优先 | 已实现-待回归 | §21.3 存量「置顶」；待回归用例 | — |

## 19.5 看板与视图（22–32）

| # | 验收原文摘要 | 状态 | 证据 | 关联任务 |
|---|---|---|---|---|
| 22 | 看板/列表/流程图三视图 | 已实现-待回归 | 96fb828（第三视图并入，§6.4.1–6.4.8/6.4.10） | — |
| 23 | 视图选择记忆到偏好 | 已实现-待回归 | 8bdf137（布局/默认值持久化入 prefs board.view） | — |
| 24 | 看板多维度分组 | 已实现-待回归 | `features/board/grouping/dimensions.ts`、`useGroupingState.ts` | 归因 commit 待细查 |
| 25 | 主分组 + 次分组 | 已实现-待回归 | `features/board/grouping/`（GroupMoreMenu/SwimlaneHead）+ `toolbar.tsx:116`（主/次分组控件） | 归因 commit 待细查 |
| 26 | 折叠/展开 | 已实现-待回归 | `grouping/ArchivedGroupsSection.tsx`、SwimlaneHead + e6272f6 | — |
| 27 | 拖拽卡片跨分组 | 已实现-待回归 | `grouping/useCrossGroupDrag.ts` | 归因 commit 待细查 |
| 28 | 拖拽泳道头调整顺序 | 已实现-待回归 | grouping 子系统；**细节交互未逐一核对** | #25（真机）、待细查 |
| 29 | 筛选指定分组 | 已实现-待回归 | `toolbar.tsx:116` 分组选项 + useGroupingState | 待细查 |
| 30 | 关键路径高亮 | 已实现-待回归 | 96fb828（关键路径/阻塞链/聚焦/右键/拖拽建依赖） | — |
| 31 | 阻塞链高亮 | 已实现-待回归 | 同上 | — |
| 32 | 聚焦模式 | 已实现-待回归 | 同上 + 690c5bd（fitView/Panel 实测修复） | — |

## 19.6 需求拆解（33–41）

| # | 验收原文摘要 | 状态 | 证据 | 关联任务 |
|---|---|---|---|---|
| 33 | begin_breakdown 后 Board 自动切换页面 | 已实现-待回归 | agent-tools.ts:209（工具注册，62b0829）+ 95b7ac5（会话视图）+ WS 驱动 | #25（真机） |
| 34 | 进度实时更新 | 已实现-待回归 | agent-tools.ts:221 report_progress + `breakdown/__tests__/breakdown.test.ts` | — |
| 35 | 任务逐步出现 | 已实现-待回归 | agent-tools.ts:232 report_task_draft（BreakdownDraft `schema.prisma:350`） | — |
| 36 | 完成后展示流程图 | 已实现-待回归 | 49facf8（待确认页流程图，§7.3） | #25 |
| 37 | 用户可编辑任务/依赖/技能 | **部分实现** | web 编辑 UI 已有（49facf8 + `features/breakdown/draft-edit.ts`）；**缺口：草案写端点（REST 仅 GET/GET/confirm/cancel，`breakdown.controller.ts:21-36`）、draft_updated 事件未见实现（grep 无服务端命中）** | #22 |
| 38 | 用户确认后任务创建 | 已实现-待回归 | `breakdown.controller.ts:31` confirm + §7.8 单事务全成或全滚（controller 注释:9）；批量通道为性能口径改造 | #26（create_tasks_batch 在途） |
| 39 | 创建页淡出，任务落入需求池 | 已实现-待回归 | 49facf8（覆盖层撤销/淡出交互）+ confirm 落库；真机视觉项 | #25、#28（origin 读侧透出，来源标记） |
| 40 | Agent 可等待确认并收到结果 | 已实现-待回归 | agent-tools.ts:300 wait_for_confirmation、:290 get_creation_status（6cf7151）；**注意 §20.3-9 的 10 分钟超时返回 `pending_confirmation` 未在服务层发现（grep `pending_confirmation` 无命中）** | #21（拆解超时任务）、#25 |
| 41 | 多会话并发独立处理 | 已实现-待回归 | BreakdownSession 按会话隔离（`schema.prisma:328`）+ breakdown.test.ts | 回归时补并发用例 |

## 19.7 会话直接创建任务（42–48）

| # | 验收原文摘要 | 状态 | 证据 | 关联任务 |
|---|---|---|---|---|
| 42 | create_task 弹出轻确认卡片 | 已实现-待回归 | creation.service.ts:190（light 分支 requestConfirmation）+ 0159648（web 卡片，右下角 §20.4-11，creation-card.tsx:18） | #28 |
| 43 | 用户可创建/编辑/取消 | 已实现-待回归 | creation-card.tsx:20（[取消][编辑][创建]）+ `api/types.ts:870`（decision edit 必带修改后载荷）+ 6cf7151 decision 闭环 | — |
| 44 | 30 秒超时自动取消 | 已实现-待回归 | creation.service.ts:64（expires_at+5s 决策宽限）、creation-card.tsx:78（「已超时（30s+5s 宽限）」）、store.ts:48（409 RESOLVED） | — |
| 45 | 重复检测提示 | 已实现-待回归 | creation.service.ts:195-197 findDuplicates、:60/:143 duplicates 载荷（§8.5/§20.4-14 Jaccard） | — |
| 46 | 创建后 5 秒内可撤销 | **部分实现** | web 侧已就位（0159648：creation-card.tsx:266/291 undo 倒计时、store.ts:91 undone）；**缺口：撤销守卫——`creation.controller.ts` 仅 GET/POST/decision，无 DELETE/撤销端点；#26 在途（DELETE 撤销守卫、creation_request 通知）** | #26 |
| 47 | 支持三种模式 | 已实现-待回归 | creation.service.ts:31（direct/light/silent）、554b82d（direct/silent）+ 6cf7151（light） | — |
| 48 | 设置页可配置 | **存疑** | api 读侧已实现（creation.service.ts:187-188 `agent_creation_mode` 优先级）；**apps/web/src 全量 grep `agent_creation_mode` 无命中——设置页 UI 入口未见，疑为未登记缺口** | 待细查（不在 #21/22/26/28 队列内） |

## 19.8 技能（49–56）

| # | 验收原文摘要 | 状态 | 证据 | 关联任务 |
|---|---|---|---|---|
| 49 | 默认技能首次启动预置、只读 | 已实现-待回归 | 06a3be0（种子预置，PRD §20.5-15/验收49）+ `skills/default-skills.ts` + `default-skills.test.ts` + 55566e4（编辑器整页只读） | — |
| 50 | 用户可创建自定义技能 | 已实现-待回归 | 存量 skills CRUD + d585130（三来源语义） | — |
| 51 | 可导入三方技能（.atskill/SKILL.md/目录/客户端） | 已实现-待回归 | d585130（导入导出闭环）+ `skills/skill-markdown.ts` + 43855fe（0010 迁移）；目录扫描为存量（§21.3） | #28（origin 读侧透出） |
| 52 | 导入时冲突检测 | 已实现-待回归 | d585130 + 8b800f5（冲突口径改同 ID，r2/§20.5-17） | — |
| 53 | 导入后技能可编辑、可绑定任务 | 已实现-待回归 | §9.8.5 口径 + 存量绑定链 + 5bda5f7（skillRef 成环/自引用拦截） | — |
| 54 | 可导出为 .atskill | 已实现-待回归 | d585130（唯一 ID 导入导出闭环） | — |
| 55 | 可绑定任务、随任务下发 | 已实现-待回归 | §10.3 存量下发 + 5bda5f7；下发按唯一 id 解析 | #28 |
| 56 | 自定义/三方技能版本管理 | 已实现-待回归 | 3563cbf（版本历史/块级 diff/回滚 + GET versions/:version）+ `schema.prisma:309 SkillVersion` | — |

## 19.9 MCP 治理（57–61）

| # | 验收原文摘要 | 状态 | 证据 | 关联任务 |
|---|---|---|---|---|
| 57 | 技能可声明 MCP 依赖 | 已实现-待回归 | §12.2 + `skill-markdown.ts`（frontmatter 扩展，§21.3）命中 mcp 关键词 | — |
| 58 | 本地检查格式和可达性 | **存疑** | check_mcp_policy 工具已就位（6eae6a6 + `agent/mcp-policy.service.ts:27`）；**「可达性」是否含网络探测未在实现中确认（本地信任模型下裁决为策略面）** | 待细查 |
| 59 | 策略随技能下发 | 已实现-待回归 | `skills.dto.ts`/`skills.service.ts` mcp 策略字段命中（随 claim/get_skill 下发） | — |
| 60 | Agent 调用 MCP 前校验 | 已实现-待回归 | 6eae6a6 check_mcp_policy（调用前查询面，§12.6） | — |
| 61 | MCP 调用审计可查 | **存疑** | report_mcp_call 落点存在（`contract/agent-schemas.ts:114`、mcp-policy.service.ts:27）；**schema 无独立 McpCallLog 表，疑并入 AuditLog；audit-api grep "mcp" 无命中——查询面（REST/页面）未证实** | 待细查 |

## 19.10 页面结构（62–67）

| # | 验收原文摘要 | 状态 | 证据 | 关联任务 |
|---|---|---|---|---|
| 62 | 左导航含看板/技能/设置 | 已实现-待回归 | 9f72464 + `apps/web/src/app/sidebar.tsx` | — |
| 63 | 导航可折叠到 64px | 已实现-待回归 | `app/app.tsx:83`（--atb-nav-w 64px/200px） | — |
| 64 | 顶部为页面工具栏 | 已实现-待回归 | 9f72464 + `app/top-bar.tsx` | — |
| 65 | 任务详情抽屉不覆盖左导航 | 已实现-待回归 | `app/overlay-slot.tsx:33` + `styles/globals.css:106-126`（drawer 变量体系）；视觉结论待真机 | #25 |
| 66 | 拆解创建页覆盖内容区+工具栏 | 已实现-待回归 | 95b7ac5 + `features/breakdown/breakdown-overlay.tsx`（overlay-slot 挂载） | #25 |
| 67 | 通知中心从左侧弹出 | 已实现-待回归 | 457714a + `app/notification-center.tsx` | — |

## 19.11 存量处置（68–71）

| # | 验收原文摘要 | 状态 | 证据 | 关联任务 |
|---|---|---|---|---|
| 68 | 旧库自动迁移完整保留+备份+可回滚 | 已实现-待回归 | 642af62（目录搬迁）+ `infra/data-dir-migration.ts`（backups/、migrated_from）+ `data-dir-migration.test.ts`；0007–0014 迁移链；首启真机待验 | #25 |
| 69 | 无登录/账号切换/市场入口 | 已实现-待回归 | 82b45a7 + 63dae46 + 554957c（删 apps/cloud 及发布残留） | — |
| 70 | accounts/market_* 表移除、全表无 account_id | 已实现-待回归 | 迁移 `0007_remove_accounts_and_market` + `schema.prisma:16`（grep model 定义已无 Account/Market） | — |
| 71 | 「项目」字样全部替换为「分组」 | 已实现-待回归 | 239751e（界面文案迁移）+ 7ed1f19 + 5120324（系统信息/README 口径）；建议回归时全站文案扫描兜底 | — |

## 19.12 二轮评审新增（72–81）

| # | 验收原文摘要 | 状态 | 证据 | 关联任务 |
|---|---|---|---|---|
| 72 | 分组可手动归档；归档默认隐藏、只读、可恢复 | 已实现-待回归 | 09421f5（0011 archived_at + archive/unarchive + 只读拦截 + 看板隐藏）+ e6272f6（归档视图） | — |
| 73 | 上限 50 仅计活跃分组，恢复时校验 | 已实现-待回归 | 7ed1f19（上限 50）+ 09421f5 + `groups.test.ts` | — |
| 74 | 画布插件能力（MiniMap/Controls/框选/网格/fitView/边标签） | 已实现-待回归 | 96fb828 + 690c5bd（fitView 延时/Panel 避让 960px 实测） | — |
| 75 | 视图阈值在设置/视图可改即时生效 | 已实现-待回归 | 8bdf137（阈值/性能档位/缩放/方向/高亮/持久化，§6.4.9） | — |
| 76 | 两个同名技能并存，按唯一 id 解析 | 已实现-待回归 | 43855fe（name 去唯一）+ d585130 + 8b800f5（重名 id 消歧）+ 5bda5f7 | — |
| 77 | 同 id 重导入触发覆盖更新（新版本），不产生副本 | 已实现-待回归 | d585130（同 ID 冲突=覆盖更新）+ `schema.prisma:309` 版本链 | — |
| 78 | 归档分组任务不进 claim/list_ready 候选，取消归档恢复 | 已实现-待回归 | adb5363（候选 SELECT + 认领 UPDATE 双谓词，§5.6 r3/验收 78） | — |
| 79 | board.create_task 决策/超时同步返回；sessions/logs 全量落库 | 已实现-待回归 | 6cf7151（light 决策闭环）+ 554b82d（会话落库）+ 0013 迁移（`schema.prisma:382/395`）+ `creation.test.ts`/`creation-rest.test.ts`；b923818（DI+冒烟门禁）；origin 读侧透出排队中 | #28 |
| 80 | 通知中心由存量数据+规则键驱动，无新增存储 | **部分实现** | 面板 UI 已就位（457714a，无新表，复用 `schema.prisma:269 Notification`）+ 0014 规则键迁移已入库；**缺口：creation_request 通知触发在途（#26），拆解/创建请求接入未闭环** | #26 |
| 81 | finish_breakdown 技能同名歧义/失败标注，确认页可见可改 | **部分实现** | 服务侧解析报告已实现（`breakdown.service.ts:36-41` 报告结构、:433-476 歧义取最近+unresolved 保留原值，62b0829/76a1410）；**缺口：GET 会话载荷未透出歧义报告（#21），确认页「可见可改」依赖 #22 草案写端点** | #21、#22 |

---

## 因轮次预算标注「待细查」的章节

1. **19.3/19.4（10–21）**：存量项以 §21.3 基线 + 既有测试文件名为据，未逐用例打开核对（尤其 14「审核必填口径」、21 置顶优先）。
2. **19.5（24–29）**：`features/board/grouping/` 子系统文件级证据充分，但未逐 commit 归因，泳道头拖拽排序（28）与分组筛选（29）的交互细节未核。
3. **48（设置页创建模式 UI）**：web 侧 grep 无 `agent_creation_mode`，需专查确认是否真缺口（不属已知排队项）。
4. **58/61（可达性探测 / MCP 审计查询面）**：落点与查询链路未逐行核对。
5. **§16.2 REST 清单 ↔ 实际路由逐条比对**：未做。
6. **§17 非功能需求（性能指标）**：不属 §19 条款，未盘点。
7. **20.3-8 窗口弹出策略（温和置顶 2 秒 + 托盘闪烁 + 系统通知）**：桌面端真机项，未核。

## 已知在途/排队项对照（引用任务板）

- **#26（在途，apps/api）**：DELETE 撤销守卫、creation_request 通知、create_tasks_batch → 影响条款 46、80（38/39 为性能口径非阻塞）。
- **#28**：origin 读侧透出 → 影响条款 39/42/51/55/79 的来源标记展示（§13.10）。
- **#21**：拆解超时任务（10 分钟 pending_confirmation / 7 天 interrupted）+ GET 歧义载荷 → 影响条款 40、81。
- **#22**：草案写端点、confirm 撤销、draft_updated → 影响条款 37、81。
- **#25**：W7b+W8-web 真机 11 项补验 → 覆盖条款 1、4、33、36、39、65、66、68 等真机/视觉项。
- **§7.8 已裁定**：「延迟 5 秒提交」口径满足条款，不再作为缺口跟踪。
