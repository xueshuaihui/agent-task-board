# v0.0.4 PRD 验收条款 ↔ 实现矩阵（#13 全量回归底稿）

> QA 子 agent 编制（任务 #29）。依据：`docs/v0.0.4/需求.md` §19（验收标准，共 81 条，19.1–19.12）+ v0.0.3..HEAD 42 个 commit + 代码现状只读核查。
> 状态口径：B1/B2/B3 三轮回归 + 主 agent 裁定回填后，本表为**收官终态**；「已实现-待回归/存疑/部分实现」等底稿态仅存历史记录处。
> apps/api 有 agent 在途（#26 等），本文档只做只读盘点，不改动任何代码。

## 统计汇总（共 81 条，B3 收官终态）

| 状态 | 计数 | 条款 |
|---|---|---|
| 通过 | 72 | 2、5–8、10–13、15–21、23–39、41–57、59–80 |
| 通过-口径差异 | 6 | 3、9、14、22、40、58 |
| 通过-口径更新 | 1 | 81（重选即消歧 f853b5c + B 通道重报 ee1f942 新拍板口径） |
| 顺延 #14（桌面打包） | 2 | 1、4 |
| 失败 | 0 | — |

> 另有真机视觉补验清单与 #14 销账清单，见文末「B3 批次记录 + 全矩阵总盘点」。

---

## 19.1 基础（1–5）

| # | 验收原文摘要 | 状态 | 证据 | 关联任务 |
|---|---|---|---|---|
| 1 | 双击桌面应用启动，托盘图标出现 | 通过—顺延 #14 | B3 回填（主 agent 裁定）：双击启动/托盘属桌面打包真机项，beta 包真机轮销账；`apps/desktop/src-tauri/resources/sidecar/` 打包链在位（B1 复核） | #14 |
| 2 | 无需登录直达主界面 | 通过 | `apps/api/src/auth/auth.guard.ts:16,60-62`：UI 凭证=本地会话 Token（ATB_UI_TOKEN/dev 落盘）直通、无账号体系；apps/web 全量 grep login 仅 `lib/phase.ts` 一处无关命中；B1 实测：落盘 token 直连 REST 全程 2xx | — |
| 3 | 浏览器可开 `http://127.0.0.1:7788/` | 通过-口径差异 | `apps/api/src/common/paths.ts:6` DEFAULT_PORT=7788 + `common/__tests__/paths.test.ts:33,63`（勘误：底稿锚点 `paths.test.ts` 实际在 common/__tests__/）；B1 实测：7788 sidecar 在听（GET / 返回 fastify JSON 404=服务可达），但根路径不托管 UI 页面——生产 UI 走 Tauri `frontendDist: ../../web/dist`（tauri.conf.json:7）、dev 走 vite:5173。「打开 7788 见主界面」字面口径与实现（API 端口≠UI 入口）差异 | — |
| 4 | 关闭浏览器后服务继续运行 | 通过—顺延 #14 | sidecar 独立进程常驻（`main.ts:89-92` listen + ATB_READY 握手）；B1 佐证：dev sidecar 全程无浏览器依赖存活；关窗常驻/托盘形态随 beta 包真机销账 | #14 |
| 5 | Agent 未接入时可正常创建/拖拽/配置任务 | 通过 | `apps/api/src/__tests__/state-machine.test.ts`、`acceptance-gaps.test.ts` B1 ls 复核在位；B1 实测任务/分组 REST 写链路全程无 Agent 依赖 | — |

## 19.2 分组（6–9）

| # | 验收原文摘要 | 状态 | 证据 | 关联任务 |
|---|---|---|---|---|
| 6 | 可创建/编辑/删除分组 | 通过—实测 | B1 实测（写仅落 /tmp/atb-verify-25 数据目录）：POST /api/v1/groups 200 → PATCH 改名 200 → DELETE(strategy=migrate) 200 `{deleted:true}`；`groups/__tests__/groups.test.ts` 背书；7ed1f19（上限 50） | — |
| 7 | 默认分组不可删除 | 通过—实测 | B1 实测：DELETE grp_default → 409 `GROUP_DEFAULT_PROTECTED`「默认分组不可删除」；`groups.service.ts` remove() isDefault 守卫（:244-248）+ :144 预置标记只认迁移植入 | — |
| 8 | 删除分组时任务迁移到默认分组 | 通过 | `groups.service.ts:252-259` migrate 分支 `task.updateMany` 迁组（目标含默认组，由前端对话框默认项显式传入）+ `groups.test.ts:95-107`「strategy=migrate 把任务迁去目标分组」用例；B1 实测删除响应带 `affected_tasks` 计数（临时组 0 任务，非空迁移以用例背书）；B3 实测：migrate 删除返回 `affected_tasks:1`（非空迁移路径直接实证） | — |
| 9 | 分组管理在设置页 | 通过-口径差异 | B3 回填（主 agent 裁定，2026-09-21 用户拍板，不复议）：独立「分组」页即达标；v0.0.4 W9 左导航口径下 `app/sidebar.tsx:13-14` 注释自证（PRD §13.2 只列看板/技能/设置，分组为保留入口的独立页）。原文「设置页」字面口径记差异。证据：`router.tsx:17` `/groups` + `app.tsx:36` GroupsPage | — |

## 19.3 任务管理（10–17，存量回归为主，见 §21.3）

| # | 验收原文摘要 | 状态 | 证据 | 关联任务 |
|---|---|---|---|---|
| 10 | 可创建/拖拽/配置任务 | 通过 | 存量看板 + `apps/web/src/features/board/mutations.ts`（复核在位）；B1 实测任务写链路正常（audit 见 task_create/task_update 序列） | — |
| 11 | Agent 通过 MCP 领取任务 | 通过—实测 | B1 POST /mcp `tools/list` 实测返回 `claim_next_task`（存量 Agent 工具全名单在位，无前缀命名）；`agent/__tests__/claim.test.ts`、`__tests__/mcp-http-flow.test.ts` 复核在位 | — |
| 12 | 领取后自动进入执行中，看板实时更新 | 通过 | `claim.service.ts` CLAIM_UPDATE_SQL 条件 UPDATE 直接 `status='RUNNING'`；`apps/web/src/ws/invalidate.ts:86-88` 事件失效 boardRoot/tasksRoot 即时重拉；`__tests__/claim-lease-flow.test.ts` 复核在位 | — |
| 13 | 完成后进入待审核 | 通过 | `agent/writeback.service.ts:147,174`「Agent 已完成执行，进入待审核」+ `agent/__tests__/writeback.test.ts` | — |
| 14 | 用户审核必填建议/原因/详情 | 通过-口径差异 | `contract/schemas.ts:116-143` reviewSchema.superRefine：**REJECT 必填** suggestion/reason/detail（:131-138），**APPROVE 三字段选填**（:119-122 注释：4.3 口径「避免通过也要写意见」误伤）；PRD 原文未区分通过/驳回 → 必填仅驳回侧成立，实现有意为之，记口径差异 | — |
| 15 | Agent 下次领取能读到审核意见 | 通过—实测 | `agent/agent-query.service.ts:34-51,78-79` review_feedback 读侧；B1 tools/list 实测含 `get_review_feedback`；`writeback.test.ts` 背书 | — |
| 16 | 两个 Agent 同时领取只有一个成功 | 通过 | `agent/claim.service.ts:126-135` 事务内 `$executeRawUnsafe(CLAIM_UPDATE_SQL)` 条件认领（外层重复可领谓词）+ `changed===0 continue` 让位下一候选 = 并发互斥；`claim.test.ts` | — |
| 17 | 租约超时回到异常/失败 | 通过 | `agent/__tests__/lease-sweeper.test.ts` + `__tests__/lease-sweep-timer.test.ts`、`lease-invalidation.test.ts` B1 ls 复核在位 | — |

## 19.4 依赖（18–21，存量回归）

| # | 验收原文摘要 | 状态 | 证据 | 关联任务 |
|---|---|---|---|---|
| 18 | 依赖未满足领不到 A | 通过 | `claim.service.ts` CANDIDATE_SELECT（:224-239）：`NOT EXISTS (SELECT 1 FROM task_dependencies … d.type='blocks' AND dep.status != 'DONE')` 在 SQL 层排除 | — |
| 19 | B 完成后 A 可领 | 通过 | 同上谓词：上游转 DONE 即自动放行；`jobs/dependency-unlock.service.ts:45-70` 解锁侧 + `__tests__/dependencies.test.ts` | — |
| 20 | 循环依赖被拒绝 | 通过 | `tasks/tasks.service.ts:590` 自依赖拦截、:686 `DEPENDENCY_CYCLE` 抛出 + `__tests__/dependencies.test.ts` | — |
| 21 | 置顶任务优先 | 通过 | CANDIDATE_SELECT `ORDER BY t.pinned DESC, t.priority ASC, t.created_at ASC`（claim.service.ts:239，claim 与 list_ready 共用同一 SELECT） | — |

## 19.5 看板与视图（22–32）

| # | 验收原文摘要 | 状态 | 证据 | 关联任务 |
|---|---|---|---|---|
| 22 | 看板/列表/流程图三视图 | 通过-口径差异 | 三形态齐：看板列/泳道（`board/index.tsx:95`）+ 流程图（`flow/FlowBoardView.tsx`）+ 任务列表；唯「列表」是独立「任务」页而非看板显示切换——`toolbar.tsx:35` 注释自证（原型 3.4 口径，与条款 9 同类入口口径，96fb828） | — |
| 23 | 视图选择记忆到偏好 | 通过 | `board/index.tsx:75`「显示模式记忆在 prefs `board.view`（§6.4.9）」；`settings/tabs/view.tsx`、`flow/view-prefs.ts` B1 grep 复核读写在位；8bdf137 | — |
| 24 | 看板多维度分组 | 通过 | `features/board/grouping/` 子系统 B1 ls 复核：dimensions.ts / useGroupingState.ts / grouping.ts / Swimlane*.tsx 等 10 文件在位 | — |
| 25 | 主分组 + 次分组 | 通过 | `useGroupingState.ts:24,41`（primary 默认「状态」+ secondary 维度）+ `toolbar.tsx:114-116` 分组面板「主分组维度、次分组与分组选项（7.7）」 | — |
| 26 | 折叠/展开 | 通过—真机轮已过 | B3 回填（主 agent 裁定）：真机轮已实测泳道单折/全折/全展（折叠后卡片隐藏、aria 翻转）。代码侧 `Swimlane*.tsx`/`useGroupingState.ts`/`ArchivedGroupsSection.tsx` collaps* 链 + toolbar「全部折叠展开」（toolbar.tsx:50）在位 | #25（已过） |
| 27 | 拖拽卡片跨分组 | 通过 | `grouping/useCrossGroupDrag.ts` B1 复核在位 | — |
| 28 | 拖拽泳道头调整顺序 | 通过—真机轮已过 | B3 回填（主 agent 裁定）：真机轮已实测 PointerEvent 拖拽换位 + 服务端 prefs `laneOrder.type` 落盘。代码侧 `useGroupingState.ts:27,274-276` setLaneOrder 重排 + persist | #25（已过） |
| 29 | 筛选指定分组 | 通过 | 分组面板「分组选项」（toolbar.tsx:114-116）+ `useFilterStore` 筛选 chip（toolbar.tsx:19,74,246 段） | — |
| 30 | 关键路径高亮 | 通过 | `flow/FlowBoardView.tsx:31,108,352` criticalPath + highlightCritical 开关；配色 `DependencyEdge.tsx:8`（3px 橙） | — |
| 31 | 阻塞链高亮 | 通过 | FlowBoardView.tsx:114 blockingChain（红色边，同上配色注释） | — |
| 32 | 聚焦模式 | 通过 | FlowBoardView.tsx:122-127 focusSet（锚点 + 上/下/全链路，范围外整卡隐藏，§6.4.7）+ 690c5bd | — |

## 19.6 需求拆解（33–41）

| # | 验收原文摘要 | 状态 | 证据 | 关联任务 |
|---|---|---|---|---|
| 33 | begin_breakdown 后 Board 自动切换页面 | 通过—真机轮已过 | B2 实测触发链（MCP 自建 token，写仅落 /tmp/atb-verify-25）：board.begin_breakdown 返回 session UUID，breakdown.started→breakdownRoot 重取→`BreakdownOverlayHost` 挂载。**真机尾款（964px 窄窗）**：Board 页 MCP begin_breakdown 后 overlay 自动打开（截图）；探针抓切换动画曲线 opacity 0→1、translateY 8→0、~200ms、left=200px 让开导航 | #25（销账） |
| 34 | 进度实时更新 | 通过—真机轮已过 | B2 实测：report_progress→`{recorded:true}`，GET progress 行会话隔离；WS breakdown.progress + invalidate 重取。**真机尾款**：report_progress step2/total4 后 receiving 页即时渲染 spinner+message「已完成技能匹配，正在切分任务」+「已完成 2/4」+ 进度条 50% 位（截图） | #25（销账） |
| 35 | 任务逐步出现 | 通过—真机轮已过 | B2 实测：report_task_draft 逐条落 drafts 读侧可见；WS breakdown.task_draft 逐条失效重取。**真机尾款**：两条草案逐条追加至「已识别任务 (2)」（截图）；draft-card 为 motion/react JS 淡入（`draft-card.tsx:32`），MutationObserver 探针抓首帧 opacity:0 + translateY:6px → 终态 opacity:1 | #25（销账） |
| 36 | 完成后展示流程图 | 通过—真机轮已过 | B3 回填：#25/#32 真机轮已实测流程图渲染。B2 实测 finish_breakdown→reviewing、GET 会话 session/drafts/progress/skill_resolution 载荷供 §7.3 确认页渲染（49facf8 + `breakdown-rest.test.ts` 8 绿） | #25（已过） |
| 37 | 用户可编辑任务/依赖/技能 | 通过—实测 | B1 疑 #22 缺口确认闭环：`breakdown.controller.ts` 具备 POST/PATCH/DELETE drafts + regenerate 四端点；`breakdown-draft-write.test.ts` **14/14 绿**（B2 本轮跑：删草案级联清悬空依赖、成环/自引用/未知前置拒绝、confirm 后 409、Agent Token FORBIDDEN、条款 81 重选落 resolved 写侧）；B2 实测：UI 凭证 PATCH d1 标题/skill_ids/acceptance 生效且随 confirm 落库（T-1018 标题=「甲任务一(用户改)」）。「draft_updated」事件实名 `breakdown.task_draft`，用户写路径 `breakdown.service.ts:468/:553/:582/:613` 均 emit、web 失效 breakdownRoot | #22（闭环） |
| 38 | 用户确认后任务创建 | 通过—实测 | B2 实测：POST confirm→completed，父 T-1017（type=需求）+ 子任务 T-1018/19、d2→d1 依赖随建；create_tasks_batch（§8.7/#26）**已落地**：`mcp/agent-tools.ts:278` 注册 + `creation.service.ts:211`（批量缺省异步 wait:false，避免 N×35s 串行阻塞），`mcp-tools.test.ts`/`mcp-http-flow.test.ts`(26)/`creation.test.ts`(19) 本轮全绿背书 | #26（闭环） |
| 39 | 创建页淡出，任务落入需求池 | 通过—真机轮已过 | B3 回填：#25/#32 真机轮已实测 confirm 后创建页淡出 Toast + 📋 来源标记。B2 实测：confirm 新建三任务 status 均 BACKLOG（需求池）；origin_type 读侧透出（`task.dto.ts:36/:119/:245`）。口径观察：拆解建任务 origin_type='user'（`breakdown.service.ts:406-409`，0013 CHECK 词表仅 user/agent），📋 标记依会话关联展示层派生 | #25（已过） |
| 40 | Agent 可等待确认并收到结果 | 通过-口径差异 | `creation/creation.service.ts:273-277` waitFor 阻塞 `entry.done` 至收口、:397 收口态 `status:'timeout'`；:154 DECISION_GRACE_MS=5s、:335 `light_confirm_timeout_seconds`、:350-356 expires+宽限 deadline 与 timer expire；:263-270 过宽限就地收口后决策 409 拒收。工具注册 `mcp/agent-tools.ts` board.wait_for_confirmation/get_creation_status（底稿 :290/:300 行号已旧，B1 tools/list 实测两工具在位）。**拍板口径：维持现实现（阻塞到轻确认超时+5s 返回 timeout），§20.3-9 十分钟 pending_confirmation 降级不采纳** | #21、#25 |
| 41 | 多会话并发独立处理 | 通过—实测 | B2 实测：S1/S2 双会话交错上报（progress/drafts 交叉发送），读侧载荷各自独立不互串；S1→confirm 走 completed、S2→cancel 走 cancelled 互不影响；`breakdown-draft-replay.test.ts` 5 绿（重放幂等侧）| — |

> **B1 批次记录（2026-09-21，QA-B1，HEAD ee1f942）**
> 覆盖：19.1–19.6 全部 41 条款 + 越节裁定项 40（本表）/58/61（19.9）。计数：**通过 28**（其中 1、4、26、28 四条标「真机轮（主 agent）」补验；6、7、11、15、40 含 B1 REST/MCP 实测证据）、**通过-口径差异 4**（3、14、22、40，另 58）、**失败 0**、**存疑-补单 2**（9：分组管理入口在独立页非设置页；61：审计页面无 MCP 类型标签）、**转B2 9**（33–39、41 + 重选消歧相关条款）。
> 实测环境注：REST 写仅落 /tmp/atb-verify-25（临时分组已删、b1-qa-agent token 已 DELETE 吊销、report_mcp_call/分组操作留审计痕迹），MCP 面走 POST /mcp；真库 ~/.agent-board 零写入；未跑 api 全量 vitest（并发隔离），全部走文件级复核。

## 19.7 会话直接创建任务（42–48）

| # | 验收原文摘要 | 状态 | 证据 | 关联任务 |
|---|---|---|---|---|
| 42 | create_task 弹出轻确认卡片 | 通过—实测+真机轮已过 | B3 回填：#25/#32 真机轮已实测右下角轻确认卡片弹卡（`creation-card.tsx`，0159648 §20.4-11）。B2 实测：board.create_task（缺省参数，设置 agent_creation_mode=light 生效）wait:false→`{created:false,pending:true,request_id,expires_at=+30s}`；GET /creation-requests 列出 title/group/duplicates 载荷；WS agent.task_requested 驱动 | #28（origin 已透出，见 39） |
| 43 | 用户可创建/编辑/取消 | 通过—实测 | B2 实测：decision 入参键为 `action`（词表 create|edit|cancel，VALIDATION 探针实证）；create→`{status:created,task_id:T-1021}`、cancel→cancelled；终结态后再决策 409 CREATION_REQUEST_RESOLVED 拒收；edit 必带修改后载荷（`api/types.ts:870`）+ `creation.test.ts` 决策用例绿 | — |
| 44 | 30 秒超时自动取消 | 通过—实测 | B2 实测：两条 light 请求 30s 未决自动转 status=timeout（task_creation_logs 落 user_action=timeout），到期决策拒收；`creation.test.ts`「超时与宽限（30s+5s，宽限内决策仍有效；到期记 timeout）」用例绿（本轮 7.2s）；`creation.service.ts:64/:143` | — |
| 45 | 重复检测提示 | 通过—实测 | B2 实测：同分组 5 分钟内命中既有任务标题→请求载荷 `duplicates:[{task_id:T-1020,similarity:1}]`（`creation.service.ts:195-197` findDuplicates，§8.5 Jaccard）；direct 重复命中升轻确认为 B1/主 agent 真机已证 | — |
| 46 | 创建后 5 秒内可撤销 | 通过—实测 | 底稿「部分实现」缺口（#26）闭环：§8.7 r3 口径撤销=UI 调存量 `DELETE /api/v1/tasks/{id}`，服务端守卫 origin+未领取（时限归前端，`tasks.service.ts:510-578` W8-a3）；B2 实测：light→action=create→T-1021（origin_type=agent、BACKLOG、未领取）→DELETE→`{undone:true}`，task_creation_logs 同任务补记 `ui|cancelled` 流水；user 来源 T-1018 DELETE→`undone:false` 不误记；`creation.test.ts:622-635` 绿 | #26（闭环） |
| 47 | 支持三种模式 | 通过—实测 | B2 实测：参数口径 direct→即时创建不确认（T-1020，log `mcp|direct|created`）；light→pending（见 42）；silent→仅创建+通知（`creation.test.ts:541-570` silent→带 task_id 的 creation_request 通知用例绿）；生效优先级 参数>设置>默认 light（`creation.service.ts:187-188`，§8.2 口径 B1 真机已证）+ 554b82d/6cf7151 | — |
| 48 | 设置页可配置 | 通过 | 底稿「存疑/疑缺口」解除：`apps/web/src/features/settings/tabs/general.tsx:189-192` 创建模式选择器读写设置 `agent_creation_mode`（`api/types.ts:562` 三态枚举）；B2 实测缺省走 light 与服务值一致；light_confirm_timeout_seconds 同页可调（B1/主 agent 验设置页实测） | 待细查（闭环） |

## 19.8 技能（49–56）

| # | 验收原文摘要 | 状态 | 证据 | 关联任务 |
|---|---|---|---|---|
| 49 | 默认技能首次启动预置、只读 | 通过—测试背书+实测读侧 | B2 本轮跑 `default-skills.test.ts` 4 绿（06a3be0 种子/只读）；实测：GET /api/v1/skills 含 `skl_builtin_*` 前缀预置条目（如 skl_builtin_code-review）；55566e4 编辑器整页只读 | — |
| 50 | 用户可创建自定义技能 | 通过—测试背书 | B2 本轮跑 `skills.test.ts` 17 绿（CRUD）+ d585130 三来源语义 | — |
| 51 | 可导入三方技能（.atskill/SKILL.md/目录/客户端） | 通过—真机轮已过 | B3 代码级复核维持：`skills-deep.test.ts` 21 绿 + d585130（导入导出闭环）/`skill-markdown.ts`（SKILL.md 解析）/43855fe（0010 三来源迁移）；origin 读侧透出（#28）。**真机尾款（964px）**：技能库页「新建技能 ▾」下拉含「导入」分组节+三项带图标（导入 .atskill／导入 SKILL.md／从 Cursor Rules 导入）+空白新建/模板/复制共 6 项（截图）；点「导入 .atskill」开统一导入中心对话框（拖拽区+点击选择+「解析预览后由服务端导入落库，作为三方技能」文案） | #28（闭环）、#25（销账） |
| 52 | 导入时冲突检测 | 通过—测试背书 | `skills-deep.test.ts` 本轮绿（文件级背书，含冲突/覆盖用例面，未逐用例点名）+ 8b800f5（冲突口径=同 ID，r2/§20.5-17） | — |
| 53 | 导入后技能可编辑、可绑定任务 | 通过—实测 | B2 本轮跑 `skill-reference.test.ts` 5 绿（绑定/成环拦截 5bda5f7）；实测：草案 PATCH skill_ids=[真实技能 id] 落定并随 confirm 建任务 | — |
| 54 | 可导出为 .atskill | 通过—测试背书 | d585130（唯一 ID 导入导出闭环）+ `skills-deep.test.ts` 本轮绿（文件级） | — |
| 55 | 可绑定任务、随任务下发 | 通过—实测 | §10.3 下发按唯一 id 解析 + `skills-deep/skill-reference` 本轮绿；实测：confirm 后任务携带绑定技能读回 | #28（闭环） |
| 56 | 自定义/三方技能版本管理 | 通过—测试背书 | 3563cbf（版本历史/块级 diff/回滚 + GET versions/:version）+ `schema.prisma:309` SkillVersion + `skills-deep.test.ts` 本轮绿 | — |

## 19.9 MCP 治理（57–61）

| # | 验收原文摘要 | 状态 | 证据 | 关联任务 |
|---|---|---|---|---|
| 57 | 技能可声明 MCP 依赖 | 通过 | §12.2 frontmatter 扩展（`skill-markdown.ts` mcp 面，B1 grep 命中）+ `skills-deep.test.ts` 21 / `mcp-tools.test.ts` B2 本轮绿（回归背书） | — |
| 58 | 本地检查格式和可达性 | 通过-口径差异 | `agent/mcp-policy.service.ts:40-73`：裁决仅策略面两谓词——server 已声明（否则 deny_undeclared §12.4）+ tools 精确到工具级（§12.2）；文件内无任何网络/fetch/探测代码（B1 grep `fetch|http|探测|可达|network` 零命中）。**拍板口径：本地信任模型下策略面检查即过，无网络探测** → 「可达性」按口径不落实现，记通过-口径差异；B1 实测：POST /mcp check_mcp_policy（临时 agent token）返回 deny_undeclared 裁决 JSON 且落审计（action=mcp_policy_check, target_type=mcp） | 待细查（闭环） |
| 59 | 策略随技能下发 | 通过 | `skills.dto.ts`/`skills.service.ts` mcp 策略字段随 claim/get_skill 下发（B1 锚点）；skills 系测试 B2 本轮全绿（回归背书） | — |
| 60 | Agent 调用 MCP 前校验 | 通过—实测（B1） | B1 实测 tools/list 含 check_mcp_policy、POST /mcp 返回 deny_undeclared 裁决并落审计；`mcp-policy.service.ts:40-73` + 6eae6a6；B2 复核文件在位 | — |
| 61 | MCP 调用审计可查 | 通过 | B3 回填（主 agent 裁定）：#36 补标签 commit 97016a3 落地，真机确认审计页渲染「MCP 调用上报/MCP 策略裁决」中文标签，labelOf 未知兜底零触发。链路 B1 实测：report_mcp_call→`{recorded:true}`→`mcp-policy.service.ts:94-103` AuditService.record（action=mcp_call，并入 audit_logs 无新表）；GET /api/v1/audit?action=mcp_call 命中（含 policy_decision:unchecked） | #36（闭环） |

> **B2 批次记录（2026-09-21，QA-B2，HEAD 4d6860c）**
> 覆盖：19.7–19.9 全部 20 条（42–61）+ B1 移交 19.6 尾部 8 条（33–39、41），共 28 条；其中 58（通过-口径差异）/61（存疑-补单）系 B1 越节裁定/登记项，B2 不复核不推翻；60 引用 B1 实测。计数：**通过 26**（33–39、41–48、49–57、59、60——其中 §7 全链路与 §8 三模式/决策/超时/重复/撤销为 B2 dev MCP+REST 全实测，19.8/19.9 尾部为 B2 本轮 vitest 背书：apps/api 16 个测试文件 171 用例全绿（breakdown 系 7 文件——draft-write 14/draft-replay 5/user-disambiguation 3/breakdown 3/breakdown-rest 8/skill-resolution 2/board-tools；creation.test 19/creation-rest 6；card-dto 4；skills 系 4 文件——default-skills 4/skills 17/skills-deep 21/skill-reference 5；mcp-tools；mcp-http-flow 26）；`breakdown-draft-write.test.ts` 14/14 确认 #22 缺口闭环），**失败 0**、**存疑 0**、**转B3 0**。
> 缺口状态更新：#22（草案写端点/编辑事件）→闭环；#26（create_tasks_batch、DELETE 撤销守卫 W8-a3、creation_request 通知留痕 W9）→三项全落地闭环（条款 38/46/80 佐证，80 本体留主表）；#28（origin_type 读侧透出 W8-a4）→闭环（条款 39/42/51/55 关联解除）；46 由「部分实现」、48 由「存疑」翻正。
> 需真机轮清单（B2 内视觉部分）：33 自动切换拆解页、34/35 进度条与卡片淡入动画、36 流程图渲染、39 创建页淡出 Toast+📋 来源标记、42 右下角轻确认卡片弹卡、51 导入入口形态（目录扫描/客户端）。**（B3 更新：36/39/42 真机轮已过销账；33/34/35/51 已于尾款轮 964px 窄窗全过销账，见文末总盘点。）**
> 口径观察（不阻断）：①底稿所称「draft_updated 事件」实名 `breakdown.task_draft`，agent/用户写路径统一 emit；②拆解确认建任务 origin_type='user'（0013 CHECK 词表仅 user/agent），§13.10「📋 拆解创建」图标须展示层依 session/parent 关联派生，随 #25 视觉验。
> 实测环境注：简报给出的 agent token（atb_Az5K…）在临时库已失效（凭证无效），B2 经 POST /tokens 自建 b2-qa-agent/b2-qa-undo 两枚、收尾均 DELETE 吊销；写仅落 /tmp/atb-verify-25（新增会话 S1 completed/S2 cancelled、任务 T-1017/19/20 留存，T-1018/T-1021 为撤销与删除守卫实测已删，creation/notifications/audit 留痕），真库 ~/.agent-board 零写入、只读亦未用。临时分组未创建。

## 19.10 页面结构（62–67）

| # | 验收原文摘要 | 状态 | 证据 | 关联任务 |
|---|---|---|---|---|
| 62 | 左导航含看板/技能/设置 | 通过 | B3 实核 `app/sidebar.tsx:10-14,62-87`：NAV 五项（看板/分组/技能/审核/设置）超集覆盖条款三项，:13-14 注释自证「PRD 13.2 只列三项、保留存量入口」口径；折叠态图标+title 提示（:67）；9f72464 | — |
| 63 | 导航可折叠到 64px | 通过—真机轮已过 | `sidebar.tsx:29,37` collapsed 态 w-16（=64px）↔ w-[200px]，:45-47 折叠按钮 aria-expanded/aria-label；`app/app.tsx:78-84` --atb-nav-w 64px/200px 全局联动；`app/store/shell.ts:54-76` 折叠态本地持久化；9f72464。**真机尾款**：点「折叠导航」实测 aside 200px→64px、--atb-nav-w 同步 200px→64px、aria-expanded=false、按钮 aria-label 变「展开导航」；再点展开回 200px 双向正常 | — |
| 64 | 顶部为页面工具栏 | 通过 | `app.tsx:23,90`：壳层右侧上下分「顶部工具栏 + 主内容区」，`TopBar`（`app/top-bar.tsx:40`）常驻顶栏（含通知铃铛/全局搜索入口）；9f72464（TopBar 由页内工具条收敛到壳层，-102 行重组） | — |
| 65 | 任务详情抽屉不覆盖左导航 | 通过—真机轮已过 | `components/ui/drawer.tsx:71` 抽屉 `fixed right-0 top-0` 右缘锚定 + `styles/globals.css:125-126` 宽 640/560px 分档（11.2）；桌面最小窗 960px − 展开导航 200px = 760px > 640px，几何上不触及左导航；`--radius-drawer` 仅左侧圆角（:106）；9f72464/457714a。**真机尾款（viewport 964px 窄窗）**：点 T-1001 卡片开抽屉，实测 drawerLeft=484 / drawerRight=964 / 宽 480px（窄窗取更窄档），导航右缘 200px，间距 284px 零覆盖，左导航全程可见可点（截图） | #25（销账） |
| 66 | 拆解创建页覆盖内容区+工具栏 | 通过—真机轮已过 | `features/breakdown/breakdown-overlay.tsx:100-105`：`fixed inset-y-0 right-0` + `style left:var(--atb-nav-w)`；overlay-slot 壳层单例挂载；95b7ac5。**真机尾款（964px 窄窗）**：overlay 探针实测 left=200px 从导航右缘起铺满内容区、inset-y-0 纵贯含顶部工具栏带（h-12 返回条取代工具栏），多会话 tab 条+滚动正常（截图） | #25（销账） |
| 67 | 通知中心从左侧弹出 | 通过—真机轮已过 | `app/notification-center.tsx:113-121`：面板 `fixed top-0 h-full w-[360px]`、`left:var(--atb-nav-w)` 贴导航右缘、初始 x:'-100%' 自左滑入；457714a。**真机尾款**：MutationObserver 探针抓滑入曲线 transform x −360px→0（即 −100%→0）、~230ms、left=200px 贴导航右缘，backdrop 压暗其余区域（截图） | — |

## 19.11 存量处置（68–71）

| # | 验收原文摘要 | 状态 | 证据 | 关联任务 |
|---|---|---|---|---|
| 68 | 旧库自动迁移完整保留+备份+可回滚 | 通过—测试背书（真库首启—顺延 #14 真机） | B3 本轮跑 `data-dir-migration.test.ts` **5 绿** + `groups.test.ts` 21 绿；642af62 + `apps/api/src/infra/data-dir-migration.ts`（backups/ 备份、migrated_from 标记回滚对账）；`apps/api/prisma/migrations/` 0001–0014 全链 14 件在位（B3 ls 实核）；真库 ~/.agent-board（停 0006）首启搬迁已实现未真机执行——随 #14 beta 包场景销账 | #14（首启真机） |
| 69 | 无登录/账号切换/市场入口 | 通过 | B3 全站扫描：apps/web tsx 「市场/账号切换」非注释命中 0（`app/app.tsx:61` 注释自证「纯本地单用户，无登录门禁」；`settings/index.tsx:18`「v0.0.4 W1a 移除服务端市场 Tab」；templates.tsx:325「登录页白屏」仅为标题前缀示例文案非入口）；`apps/` 目录仅剩 api/desktop/web（554957c 删 apps/cloud）；82b45a7 + 63dae46 | — |
| 70 | accounts/market_* 表移除、全表无 account_id | 通过 | B3 实核 `apps/api/prisma/migrations/0007_remove_accounts_and_market/` 在位 + `schema.prisma:16` 注释「全表去 account_id、删 accounts 与 market_* 表；本地单用户，鉴权只认 token」；grep model 定义无 Account/Market（底稿复核维持） | — |
| 71 | 「项目」字样全部替换为「分组」 | 通过 | B3 全站扫描兜底完成：`apps/web/src` 全量 ts/tsx grep「项目」**零命中**（含注释）；239751e（界面文案）+ 7ed1f19 + 5120324（系统信息/README）+ 迁移 `0008_rename_projects_to_groups`（B3 ls 在位） | — |

## 19.12 二轮评审新增（72–81）

| # | 验收原文摘要 | 状态 | 证据 | 关联任务 |
|---|---|---|---|---|
| 72 | 分组可手动归档；归档默认隐藏、只读、可恢复 | 通过—实测（B3） | B3 dev REST 实测（写仅落 /tmp/atb-verify-25，临时组 b3-qa3 已收尾删除）：POST /groups/{id}/archive→`status:ARCHIVED`+`archived_at` 对账；GET /groups 活跃列表**不含**归档组、`?archived=true` 可见（`groups.service.ts:71-76`）；**只读守卫双向实证**：向归档组 POST /tasks→409 `GROUP_ARCHIVED`、PATCH 移动存量任务进归档组→`GROUP_ARCHIVED`（`tasks.service.ts:1140-1152` assertGroup，create:120/patch:179 共用）；unarchive→恢复 ACTIVE；看板隐藏 `tasks.service.ts:1452-1455`（显式 group_id 过滤仍可见=归档折叠区口）；`groups.test.ts` 21 绿本轮；09421f5/e6272f6 | — |
| 73 | 上限 50 仅计活跃分组，恢复时校验 | 通过 | `groups.service.ts:76` 列表缺省只查 `status:'ACTIVE'`（活跃口径）+ :131 注释「反归档上限校验、archived_at 对账」——unarchive 路径显式过上限校验防恢复突破 50；`groups.test.ts`（21 绿本轮）含上限面用例；7ed1f19 + 09421f5 | — |
| 74 | 画布插件能力（MiniMap/Controls/框选/网格/fitView/边标签） | 通过 | `features/board/flow/FlowBoardView.tsx:3-10` 导入 Background(Variant)/MiniMap/Controls/SelectionMode；:42-44 能力清单注释（框选多选/selectable-draggable-deletable/fitView+zoomToExtent/snapToGrid/边 label/自定义 Handle/键盘/拖拽连线建依赖）；:217-223 fitView 真机 960×720 实测校正（120ms 延时+padding）；96fb828 + 690c5bd | — |
| 75 | 视图阈值在设置/视图可改即时生效 | 通过 | `features/settings/tabs/view.tsx:12,54-131`：方向/节点简化阈值/缩放上下限/关键路径与阻塞链高亮开关，`normalizeViewPrefs` 非法值兜底；即时生效链=prefs 写→`flow/view-prefs.ts` 读侧订阅（与条款 23 同一 prefs 体系，B1 grep 读写复核在位）；8bdf137（§6.4.9） | — |
| 76 | 两个同名技能并存，按唯一 id 解析 | 通过 | 43855fe（0010：name 去唯一约束→同名可并存）+ d585130（唯一 ID 闭环）+ 8b800f5（冲突口径=同 ID）；解析侧 `breakdown.service.ts:731-774`：唯一命中→按 id 落定、同名多技能→取 updatedAt 最近者并记 ambiguous（:825/:838 `state: 'ambiguous'`）、无命中→unresolved 保留原值；绑定/成环 `skill-reference.test.ts` 5 绿；`skills-deep.test.ts` 21 绿（B2 本轮） | — |
| 77 | 同 id 重导入触发覆盖更新（新版本），不产生副本 | 通过 | d585130（同 ID 冲突=覆盖更新新版本非副本，r2/§20.5-17）+ `schema.prisma:309` SkillVersion 版本链 + 3563cbf（版本历史/块级 diff/回滚 + GET versions/:version）+ `skills-deep.test.ts` 冲突/覆盖用例面（21 绿文件级，B2 本轮） | — |
| 78 | 归档分组任务不进 claim/list_ready 候选，取消归档恢复 | 通过 | B3 实核 `agent/claim.service.ts:220-230,258-260`：候选 SELECT `AND (g.id IS NULL OR g.status != 'ARCHIVED')` + 认领 UPDATE 同谓词（NOT EXISTS 归档组子查询）**双保险**（:220 注释点名「验收 78，§5.6 r3」）；取消归档谓词自然放行（组 status 回 ACTIVE，B3 实测 unarchive→ACTIVE 链路）；`claim.test.ts` +55 行用例（adb5363） | — |
| 79 | board.create_task 决策/超时同步返回；sessions/logs 全量落库 | 通过—实测 | B2 实测全链：light→同步返回 `{pending:true,request_id,expires_at:+30s}`、decision create→`{status:created,task_id}`（T-1021）、30s 未决→task_creation_logs 记 user_action=timeout、终结态决策 409；origin 读侧已透出（#28 闭环）；0013_add_creation_session_logs 迁移在位（B3 ls）+ `schema.prisma:382/395` + `creation.test.ts` 19/`creation-rest.test.ts` 6 绿（B2 本轮）；6cf7151/554b82d/b923818 | #26（闭环） |
| 80 | 通知中心由存量数据+规则键驱动，无新增存储 | 通过—实测（B3） | 底稿「部分实现」缺口（#26）闭环：`creation.service.ts:174-175` §13.9 r3「creation_request 规则键站内通知落库处（复用存量 notifications 模块，不新增存储）」、:364 pending 时 push、:581 创建后 push（:363 注释：task_id 外键约束下请求 id 走 NULL、W9 铃铛白名单此 kind）；B3 实测 GET /api/v1/notifications 返回 23 条、kind 全为 `creation_request`（B2 全链路留痕直接佐证）；`migrations/0014_notification_creation_request` 在位；面板 457714a 复用 `schema.prisma:269` Notification 无新表 | #26（闭环） |
| 81 | finish_breakdown 技能同名歧义/失败标注，确认页可见可改 | 通过-口径更新 | B3 按最新拍板口径记账（验收原文绑定旧行为部分不复议）：**重选即消歧**（f853b5c：用户 PATCH 显式技能 id→`user_disambiguated` 行标记落库、读侧把该草案 ambiguous 降级 resolved——B3 实测 GET completed 会话 drafts 字段含 `skills_status`+`user_disambiguated`）；**B 通道重报**（ee1f942：agent 单草案重报守卫放宽，`BREAKDOWN_DRAFT_REF_TAKEN` 重报通道，`breakdown-draft-replay.test.ts` 5 绿）。可见：`breakdown.service.ts:36-42` 报告结构（ambiguous: ref/name/skill_id/candidates、unresolved）、:255-277 finish 返回 skill_resolution、:658 GET 会话透出（B3 实测 S1 completed 会话载荷 keys=[drafts,progress,session,skill_resolution]，**底稿所疑 #21「GET 未透出歧义报告」缺口解除**）；web 确认页 `draft-card.tsx:14,70-82` ambiguous 黄色告警+候选名展示、`draft-editor.tsx:15,80,174-180` 重选=PATCH skill_ids（歧义时保留同名兄弟选项防 filter 遮蔽）→ **可见可改闭环**；`breakdown-user-disambiguation.test.ts` 3 绿（B2 本轮） | #21、#22（闭环） |

---

## 因轮次预算标注「待细查」的章节（收官状态）

1. **19.3/19.4（10–21）**：（已闭环）B2 本轮跑齐 claim/mcp-http-flow/creation/skills 系等 16 文件 171 用例全绿，条款 14 口径、21 置顶 ORDER BY 均逐行核对。
2. **19.5（24–29）**：（已闭环）B3 收尾：26/28 真机轮已过（主 agent 裁定回填）；29 筛选分组入口链复核在位。
3. **48（设置页创建模式 UI）**：（已闭环）general.tsx:189-192 选择器实测在位。
4. **58/61**：（已闭环）58 策略面拍板、61 #36 补标签 97016a3 真机确认。
5. **§16.2 REST 清单 ↔ 实际路由逐条比对**：未做——不属 §19 验收条款，遗留为文档性核对项（不阻断收官）。
6. **§17 非功能需求（性能指标）**：不属 §19 条款，未盘点（同上）。
7. **20.3-8 窗口弹出策略（温和置顶 2 秒 + 托盘闪烁 + 系统通知）**：桌面端真机项——归 #14 beta 包真机轮一并销。

## 已知在途/排队项对照（引用任务板，B3 收官更新）

- **#26**→**闭环**：create_tasks_batch（:278/:211）、DELETE 撤销守卫 W8-a3、creation_request 通知落库（:364/:581，B3 实测 23 条读侧）三项全落地（条款 38/46/79/80）。
- **#28**→**闭环**：origin_type 读侧透出（条款 39/42/51/55/79）。
- **#21**→**闭环**：GET 会话歧义载荷 B3 实测透出（skill_resolution + drafts.skills_status/user_disambiguated）；超时降级部分按条款 40 拍板口径不采纳。
- **#22**→**闭环**：草案写端点 + `breakdown.task_draft`（draft_updated 事件实名）+ 条款 81 重选写侧。
- **#25**→大部分已过：26/28/36/39/42 真机轮销账；遗留视觉项见下表。
- **#36**→**闭环**：条款 61 审计页 MCP 标签（97016a3）。
- **#14**（新开工）：桌面打包 + beta 真机轮，销账条款 1、4、68 首启、20.3-8。

---

## B3 批次记录 + 全矩阵总盘点（2026-09-21，QA-B3，HEAD fc7a6c3）

**B3 覆盖**：19.10–19.12 全部 20 条（62–81）逐条闭环 + 主 agent 已裁定/已验 13 行回填（1、4、9、26、28、33、34、35、36、39、42、51、61）。
**B3 手段**：代码级逐行核（W9 壳层 sidebar/app/shell/top-bar/overlay-slot/notification-center/drawer/globals.css、breakdown-overlay、flow、settings tabs、groups/claim/tasks service、migrations 0001–0014 全链 ls、全站文案扫描双零命中）；实测（dev 服务 :7788，写仅落 /tmp/atb-verify-25）：分组归档全链（archive→活跃列表隐藏→只读守卫 GROUP_ARCHIVED 建/移双向 409→unarchive 恢复→migrate 删除 affected_tasks=1）、completed 会话 skill_resolution 载荷与 drafts skills_status/user_disambiguated 字段、notifications 23 条 kind 字典（全 creation_request）；测试本轮跑：`data-dir-migration.test.ts` 5 绿 + `groups.test.ts` 21 绿（26/26 pass）。
**B3 计数（19.10–19.12 20 条）**：通过 20、失败 0；其中实测项 72/80/81、测试背书项 68/77/78、口径更新项 81；65/66 附「视觉复验—待真机轮」尾注（几何/代码级判定已通过）。
**实测环境注**：本轮零新建 token（未走 MCP 面，复用 UI token）；临时组 b3-qa归档组/b3-qa2/b3-qa3 全部删除（migrate→grp_default），实测产生的 T-1022 已 DELETE 清理；真库 ~/.agent-board 零写入。
**观察（不阻断）**：`POST /api/v1/tasks` 对归档组守卫在 type 词表校验**之后**（探针首触 VALIDATION 非 GROUP_ARCHIVED），拦截点仍成立、仅错误序后置于其它字段校验，行为可接受。

### 全矩阵总盘点（81 条款收官计数）

| 分类 | 计数 | 条款清单 |
|---|---|---|
| **通过** | **72** | 2、5–8、10–13、15–21、23–39、41–57、59–80（33/34/35/51/63/65/66/67 尾款轮真机已过，视觉尾注全部销账） |
| **通过-口径差异** | **6** | 3（7788≠UI 入口）、9（独立分组页达标·拍板）、14（必填仅驳回侧）、22（列表为独立页）、40（超时口径拍板）、58（无网络探测·拍板） |
| **通过-口径更新** | **1** | 81（重选即消歧 f853b5c + B 通道重报 ee1f942 落地，原文旧行为绑定按新口径记账） |
| **失败** | **0** | — |
| **顺延 #14（桌面打包）** | **2** | 1（双击启动+托盘）、4（关窗常驻+托盘） |

### 待主 agent 真机轮清单（纯视觉细节，代码链均已通过）— **已销账（2026-09-21 尾款轮）**

1. 条款 33/34/35：拆解页自动切换动画、进度条实时更新渲染、草案卡片逐条淡入。→ **过**（964px 窄窗；overlay 自动打开+动画曲线 opacity 0→1/y 8→0/~200ms；进度条 2/4=50% 位+message；两草案逐条追加，探针抓淡入首帧 opacity:0+translateY:6px）。
2. 条款 51：三方技能导入入口桌面形态（目录扫描/客户端交互，解析与覆盖逻辑测试已背书）。→ **过**（「新建技能 ▾」下拉「导入」分组节含 .atskill/SKILL.md/Cursor Rules 三项带图标；点 .atskill 开统一导入中心对话框：拖拽区+点击选择+三方技能说明）。
3. 条款 65/66：抽屉与拆解 overlay 在 960px 最小窄窗的视觉复验（右缘锚定+左让导航的几何判定已核；参照「布局结论须在窄窗口复验」惯例）。→ **过**（viewport 964px：抽屉 drawerLeft=484 vs 导航右缘 200，零覆盖；overlay left=200px 铺满内容区含工具栏带）。
4. 条款 67/63：通知面板左滑入动画、导航折叠 200↔64px 过渡观感（结构/持久化/联动 CSS 变量均已实测在位）。→ **过**（通知面板 x −360→0 滑入 ~230ms 贴导航右缘+backdrop 压暗；折叠实测 200↔64px、--atb-nav-w 同步、aria 双向切换）。

**尾款轮实测注**：主 agent 于 in-app 浏览器（视口 964px）执行；MCP 面自建 token（tail-verify-qa），验证会话「尾款轮-尾款验证需求」用毕 cancel、token 用毕 DELETE；写仅落 /tmp/atb-verify-25，真库零写入。至此 §19 真机轮清单全部清零。

### #14 销账清单（beta 包真机轮）

- 条款 1、4：双击启动、托盘图标、关窗常驻。
- 条款 68：真库 ~/.agent-board（停 0006）首启自动迁移+备份+回滚对账——演练仅 /tmp 副本带 ATB_DATA_DIR，真库首启随打包场景一次性完成。
- §20.3-8：窗口弹出策略（温和置顶 2s + 托盘闪烁 + 系统通知）。
- 前置门禁：矩阵 §19 清零（本次达成）→ 可打 v0.0.4-beta.N tag 走 release.yml 出包（预发布拍板口径）。

**#14 开工条件判定：具备。** §19 全部 81 条无「失败」「存疑」「部分实现」「未实现」残留；2 条顺延项即 #14 本体工作，4 项视觉清单不阻断打包（真机轮顺带复验）。
