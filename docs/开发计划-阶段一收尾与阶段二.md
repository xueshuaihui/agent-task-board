# 开发计划：阶段一收尾 + 阶段二补齐

> 依据：`docs/Agent Task Board 产品与设计文档 v1.5.md` 第 17/18 章（阶段清单 + 43 条验收）。
> 当前基线：阶段一完成度约 90–95%，macOS `.app/.dmg` 已打通，API 侧 35 个测试文件。
> 原则：先补阶段一门禁缺口（M4 上线门禁），再按低成本→重活顺序推进阶段二（M5「43 条全绿」）。
>
> ⚠️ **本文正文的「当前状态」是 2026-09-15 的快照**；0919 批次（v0.0.2 / v0.0.3）插进来一整期需求，
> 最新状态见下面的 **§0′**，实现全景见 `docs/发版记录 v0.0.3.md`。

---

## 0′. 2026-09-20 复核：0919 批次之后的状态

| 项 | 状态 | 依据 |
|---|---|---|
| 发布通道 | ✅ 改为**推 tag 走 CI**，双架构（x64 + arm64）自动出包并发 Release；已发 `v0.0.1 / v0.0.2 / v0.0.3` 三个 prerelease | `.github/workflows/release.yml`、`docs/发布手册.md` |
| 产品更名 | ✅ Agent Task Board → **Jarvis Workbench**（`identifier` 与数据目录 `~/.agent-board` 未变） | commit `32f7b78` |
| 测试基线 | ✅ 44 文件 / 528 用例（api）+ 1 文件 / 19 用例（cloud）；typecheck 0 错误 | v0.0.3 发版记录 §4 |
| 0919 新需求 | ✅ 账号体系、BLOCKED 七状态、看板分组泳道、技能四类视图与编辑器、服务端市场对接、`apps/cloud` 独立部署 | v0.0.3 发版记录 §2（逐条带证据） |
| 服务端上线 | ⏸ 打包与部署链路已就绪，**实际部署暂停**（等服务器侧执行） | `apps/cloud/README.md`、v0.0.3 记录 §7 |
| A3 真机验收 / **首个正式版 tag** | ❌ 仍未过（从未发布过正式版；已发的都是 prerelease） | v0.0.1 记录 §5 |
| 阶段二 B1（字段三类 UI） | ❌ 仍未做：`multiselect / date / url` 在任务详情只读展示原值 | `features/task-detail/fields.tsx:10` |
| 阶段二 B2 CSV / B3 通知面板 / C1 四类预览 / C2 DAG / D1 自动备份 UI | ❌ 全部未做，五个阶段开关仍全 false | `apps/web/src/lib/phase.ts` |
| C2 依赖图后端接口 | ❌ 仍无 `/dependencies/graph` 注册 | `apps/api/src` 全文无匹配 |
| D1 自动备份 | ❌ 定时器未接：`backup_time`/`backup_keep` 只存在于设置契约，无调度器读取 | 手册 §6.2 |

> 结论：下面 B/C/D 三节的待做项**一条都还没做**（0919 批次吃掉了阶段二前的排期），
> 里程碑表按此重排即可；A 节的 macOS 发布链路已因 CI 化而比原文更完备。

---

## 0. 当前状态快照（2026-09-15 复核，全部经代码/实跑核验）　【历史快照，最新见 §0′】

| 项 | 状态 | 依据 |
|---|---|---|
| 测试基线 | ✅ 35 个测试文件 / 426 用例全绿，typecheck api+web 0 错误 | 2026-09-15 盘点实跑记录 |
| 最新提交 | `74c4105` Cursor 外部调度脚本 + 端到端覆盖 + 本计划文档 | git log（2026-09-15 20:02） |
| A3 Cursor REST 调度脚本 | ✅ 已完成 | `scripts/cursor-agent-poll.mjs`（265 行）+ `cursor-scheduler.e2e.test.ts`（真实 spawn + 真 HTTP 三条路径，验收 20 脚本路径闭环） |
| 阶段二入口开关 | 五个布尔全 false，与计划一致待开 | `apps/web/src/lib/phase.ts:7-19` |
| B2 CSV 排除口径 | 仍生效，待放开 | `apps/api/src/data/data.dto.ts` |
| B3 通知模块 | 列表/已读接口已有，面板与清理任务待做 | `apps/api/src/notifications-api/` |
| C1 预览限制 | 四类只给下载，待放开 | `apps/web/src/features/task-detail/artifacts/artifact-list.tsx` |
| C2 依赖图接口 | 无 `/dependencies/graph` 注册，待新建 | `apps/api/src/` 全文无匹配 |
| D1 自动备份开关 | 未渲染，待做 | `apps/web/src/features/settings/tabs/backups.tsx` |
| ⚠️ 打包不可复现 | ✅ 已解除并回归验证（2026-09-15 20:17）：node 二进制（110M，x86_64）拷入后 `tauri build` 成功产出 .app（`Contents/Resources/sidecar/` 含 main.js + node），.dmg 按 README 兜底用 `hdiutil` 生成（44M）；`bundle_dmg.sh` 的 osascript 失败为已知环境限制，非资源问题 | 本次实跑 |
| ⚠️ 110MB node 二进制未 ignore | ✅ 已修复（2026-09-15 20:13）：`.gitignore:20` 追加 `apps/desktop/src-tauri/resources/sidecar/`，`git check-ignore` 验证生效 | `git check-ignore -v` 实测 |

---

## A. 阶段一收尾（上线门禁，优先级最高）

### A1. Windows 打包 —— **本期挂起，移至阶段二（见 F1）**

> 决策（2026-09-15）：本期无 Windows 构建环境且签名证书未定，Windows 安装包整体顺延到阶段二实现；阶段一发版口径改为 macOS（.app/.dmg）单平台交付。原 A1 的技术要点（NSIS target、icon.ico、sidecar `node.exe` 路径适配、构建脚本）全部保留在 F1，届时直接照做。

### A2. macOS 签名与公证 —— ✅ 已按「无证书降级」路线收口（2026-09-15）
- [x] ~~有证书路径~~ 决策：本期无证书，走降级方案（证书升级路径仍文档化，见手册 2.4「升级路径」与 README）。
- [x] 无证书降级方案文档化：README「运行须知（签名与 Gatekeeper，无证书降级方案）」含 `xattr -cr` 指引 + **arm64 原生构建路径**（rustup target + arm64 node + `--target aarch64-apple-darwin`）+ 通用包说明（`universal-apple-darwin` + lipo 合成 node）；手册新增 2.4 节（签名状态、拦截解除表、升级路径）。
- [x] 验收（降级口径）：Gatekeeper 拦截属预期且解除方式已文档化；发版记录（`docs/发版记录 v0.0.1.md`）将其列为已知限制 #1。

### A3. 真机验收清单逐条过（W6 交付项）
- [x] 补齐 Cursor REST 调度脚本（文档 14 章）：`scripts/cursor-agent-poll.mjs`，走 REST `claim → heartbeat → complete` 全链路，配 `cursor-scheduler.e2e.test.ts` 端到端覆盖（commit `74c4105`，2026-09-15）。剩余：真机 launchd 挂载后跑一轮。
- [x] **真机验收已抓到并修复首个缺陷**（2026-09-15 21:15）：DMG 双击启动报 `Cannot find module '@nestjs/core'`——打包只带了 `dist/main.js`，缺 node_modules 与 dist 其余文件。修复：`npm run build:sidecar`（webpack 单文件 bundle 保留装饰器元数据 + 最小 Prisma 运行时 + 迁移目录 + node 装配，`tauri.conf.json` 资源改目录整体映射），从 .app 内路径实测 `ATB_READY` + REST 401 正常。
- [ ] 手测无法从代码确认的验收项并记录结果：
  - 1（双击启动+托盘）、4（关窗服务常驻+托盘恢复）、31（单实例）
  - 33（离线可用，外链除外）、41（铃铛+托盘角标联动）
  - 15（重启端口不变）、43 后半（历史库未知枚举显示「未知（原值）」）
- [ ] 跑一遍 `e2e-probe.sh`（空数据目录）确认主链路全绿；评估是否将其纳入 npm script / CI。
- [ ] 四家客户端接入自测：Qoder、Claude Code、Codex（MCP HTTP + Token）、Cursor（REST 脚本），验收 20。

### A4. 产出物 —— 部分完成（2026-09-15）
- [x] 更新 `docs/系统操作使用手册.md`：新增 2.4 节「安装包签名状态与 Gatekeeper 降级说明」（Windows 安装步骤随 F1 回补）。
- [x] 新增 `docs/发版记录 v0.0.1.md`（原名 v0.1.0，因正式版从未发布而改名；产物清单含 SHA-256、质量基线、已知限制、M4 门禁 checklist）。
- [ ] 首个正式版 tag：**待 A3 真机验收通过后执行**（发版记录 §5 已标注此门禁；注意 `v0.1.0` 会被 CI 判成 prerelease，正式版口径见 `docs/发布手册.md` §0）。

---

## B. 阶段二 P2-W1（低成本项先行）

### B1. 自定义字段剩余三类 UI（multiselect / date / url）
- 服务端校验已长齐（`apps/api/src/contract/custom-fields.ts`），只差前端：
- [ ] 设置页字段定义：类型下拉放开三类（`apps/web` settings/fields tab）。
- [ ] 任务创建/编辑表单：三类控件渲染（multiselect 多选框、date 日期选择、url 输入校验）。
- [ ] 卡片展示：`show_on_card` 开放范围从四类扩到含 url（文档：textarea 不上卡片）。
- [ ] 筛选面板：新增「自定义字段」分组（`custom_fields[key]` 查询参数已支持）。
- [ ] 测试：表单校验 + 筛选联动。

### B2. CSV 双向导入导出
- [ ] 后端：`apps/api/src/data/` 增加 CSV 导出（含 BOM、列定义头）与导入（映射 JSON 同一套校验，13 章错误码复用）；DTO 放开 CSV 格式（`data.dto.ts` 目前明确排除）。
- [ ] 前端：设置页「数据」Tab 放开 CSV 格式选项（`lib/phase.ts:16` `SHOW_CSV_EXPORT` 置 true）。
- [ ] 测试：CSV round-trip、BOM、错误行报告。

### B3. 通知面板
- [ ] 后端：通知列表/逐条已读接口已有（`notifications-api/`），补 30 天清理定时任务。
- [ ] 前端：铃铛点开下拉面板（列表、未读标记、逐条已读、全部已读、点击跳转）。
- [ ] 测试：清理任务、面板已读联动计数回落。

### B4. 批量打标签收尾
- [ ] 已提前实现（`batch-bar.tsx`），补测试 + 文档确认口径即可。

---

## C. 阶段二 P2-W2（重活）

### C1. 产物预览四类渲染器
- [ ] `markdown`：引入渲染库（如 marked/markdown-it + sanitize），独立预览组件。
- [ ] `json`：格式化 + 折叠树视图。
- [ ] `html`：**沙箱独立源**渲染（Tauri 下用独立 WebView 窗口或 `<iframe sandbox>` + data/blob URL + CSP），这是安全敏感项，需评审。
- [ ] `pdf`：pdf.js 集成（worker 路径在打包产物内可用）。
- [ ] 放开 `artifact-list.tsx` 中四类的预览入口（当前只给下载）。
- [ ] 超限文件仍走下载（验收 17 降级口径回补为全量）。

### C2. 依赖图 DAG 页
- [ ] 后端：注册 `GET /api/v1/dependencies/graph`（当前按文档 404），数据结构按文档 5.9。
- [ ] 前端：新路由 `/dependencies`，顶部导航加入口（`router.tsx`、`top-bar.tsx`），reactflow + dagre 自动布局，点击节点打开详情抽屉。
- [ ] 测试：图数据接口 + 前端节点连线正确性（验收 30）。

---

## D. 阶段二 P2-W3（收尾）

### D1. 定时自动备份 + 保留清理
- [ ] 后端：备份定时器（`backup_time` 默认 03:00、`backup_keep` 默认 7），文件名尾追加 `-auto` 区分来源（文档 20.8 口径）；清理按保留份数。
- [ ] 前端：设置页「备份」Tab 放开自动备份开关/频率/时间/保留份数（`backups.tsx` 当前明确无开关）；备份列表显示总占用大小。
- [ ] 测试：定时触发（可注入时钟，参照 lease sweeper 的做法）、清理边界。

### D2. 审计筛选 UI + 对象详情内嵌
- [ ] 后端：`audit-query.service.ts` 增加 subject/actor/时间筛选参数。
- [ ] 前端：设置页「日志与审计」Tab 加筛选；任务详情抽屉「审计」Tab 传 `subject_id` 复用同一列表组件（文档 17.2 口径）。

### D3. Linux 打包（deb / AppImage）
- [ ] `tauri.conf.json` 增加 linux targets（deb/AppImage），desktop 文件与图标。
- [ ] CI 或 Linux 环境构建验证；sidecar node 的 Linux 分发同 macOS 方案。

### F1. Windows 打包（NSIS 安装包）—— 自阶段一 A1 顺延
- [ ] `apps/desktop/src-tauri/tauri.conf.json`：`bundle.targets` 增加 `["nsis"]`（平台条件化：macOS 保持 app/dmg，Windows 用 nsis），补 `windows` 段配置（webviewInstallMode、语言、图标 `icon.ico`）。
- [ ] 生成/补齐 Windows 图标资源（`icons/icon.ico`，Tauri 要求多尺寸）。
- [ ] sidecar Windows 适配：`sidecar.rs` 的 `resolve_node()` 在 Windows 下找 `node.exe`；验证 `Resources/sidecar/` 在 NSIS 包内的路径解析（用 Tauri path API 而非 POSIX 硬编码）。
- [ ] 构建脚本：新增根脚本 `desktop:build:win` 与文档说明（交叉编译不可行，需在 Windows 机器或 CI runner 上执行）。
- [ ] Windows 代码签名证书（可选，视账号情况）。
- [ ] 验收：Windows 10/11 上双击安装 → 主窗口打开、sidecar 起来、建任务、托盘出现（验收 1）。

---

## E. 最终门禁（M5）

- [ ] 43 条验收标准逐条复验（含阶段二回补子集：17 全量预览、19 自动备份、26 CSV、30 DAG）。
- [ ] 阶段二清单（17.2 表）逐项对勾。
- [ ] 文档更新：README、操作手册、产品文档排期勾销。

---

## 里程碑对照

| 里程碑 | 内容 | 状态（2026-09-20） |
|---|---|---|
| A（≈M4 完成） | macOS 单平台发版（签名/公证方案）+ 验收逐条过 + 发版记录 | **进行中**：发布链路已 CI 化并出了 3 个 prerelease；余 A3 真机手测 + 四家客户端自测 + 正式 tag。Windows 顺延 |
| 0919（新插入一期） | 账号体系 / BLOCKED / 看板分组 / 技能与编辑器 / 市场对接 / `apps/cloud` 部署 | **代码已完成并发 v0.0.3**；服务端上线部署暂停中；缺口清单见 v0.0.3 记录 §3 |
| B（≈P2-W1） | 字段三类 UI + CSV + 通知面板 | 待做（`phase.ts` 五个开关仍全 false） |
| C（≈P2-W2） | 预览四类渲染器 + DAG 图 | 待做（技能侧已有 @xyflow/react 流程图，任务依赖图仍未做） |
| D（≈P2-W3） | 自动备份 + 审计筛选 + Linux 包 | 待做 |
| E（M5） | 43 条全绿（验收 1 的 Windows 子项随 F1 回补） | 最终门禁 |
| F1（P2） | Windows NSIS 打包 + 签名 | 自阶段一顺延 |

## 下一步执行顺序（2026-09-20 重排）

0. **续做被暂停的服务端上线**：`bash apps/cloud/deploy/bundle.sh` 出离线包 → scp 到服务器
   `/opt/agent-task-board-cloud` → `./deploy.sh`（80/443 被 1Panel 的 OpenResty 占着时按提示走
   `EDGE_MODE=off`，由面板反代 `127.0.0.1:7789`）→ `./deploy.sh status` 看 `/healthz`。
1. ~~A2 签名/公证决策~~ ✅ 已定（无证书降级，文档化完成）→ **A3 真机手测逐条过 + 四家客户端接入自测**
   → A4 打正式 tag（发布通道已 CI 化，见 `docs/发布手册.md` §1）。
2. 里程碑 B（P2-W1：字段三类 UI / CSV / 通知面板）→ C（预览四类 + DAG）→ D（自动备份 + 审计筛选 + Linux 包）
   → F1（Windows NSIS）→ E（M5：43 条全绿）。
3. 0919 期遗留的缺口（MCP 治理整章、离线闸门、技能推荐、服务端审核语义、前端自动化测试）
   需要先决定是「补做」还是「修订 PRD 口径」，见 `docs/发版记录 v0.0.3.md` §3。

## 风险与注意

1. **Windows 打包无法交叉编译**：需要 Windows 机器/CI runner；sidecar node.exe 分发方案先在 macOS 上把路径解析逻辑写成平台无关。
2. **HTML 沙箱预览**是安全敏感项，实现前先定方案（独立源 WebView vs iframe sandbox），避免 XSS 进主窗口上下文。
3. **pdf.js worker** 在 Tauri 打包产物（tauri:// 协议）下的路径加载需要实测。
4. **签名**依赖 Apple Developer 账号与 Windows 代码签名证书，若没有则按降级方案文档化，不影响功能验收。
5. 自动备份定时器参照 `lease.service.ts` 的可注入定时器模式，保证可测试性。
6. ~~**打包资源缺失（当前阻断 `tauri build`）**~~ ✅ 已解除（2026-09-15）：资源已补齐且路径已 ignore；注意该文件不入库，换机器打包前需按 README 重新拷贝。
7. 验收 20 的「脚本路径」已闭环（`cursor-scheduler.e2e.test.ts`），但「四家真实客户端接入」仍需真机逐家实测，不能以测试替代。
