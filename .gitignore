node_modules/
dist/
build/
target/
*.log
.DS_Store
.env
.env.local
coverage/
apps/api/prisma/dev.db
apps/api/prisma/*.db
apps/api/prisma/*.db-journal
# 云端市场 Prisma client 自定义输出（generate 产物，含引擎二进制，不入库）
apps/cloud/prisma/generated/
# 云端服务本地库（CLOUD_DATA_DIR 默认在 ~/.agent-board-cloud，仓库内兜底）
apps/cloud/*.db
.turbo/
.vite/

# TypeScript 增量构建缓存（不是源码）
*.tsbuildinfo

# Tauri 打包资源：110MB node 二进制手动拷入（见 README「前置」），不入库
apps/desktop/src-tauri/resources/sidecar/

# WorkBuddy 工作区数据（会话记忆等），不入库
.workbuddy/
